# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/
/.no-track/
/.firebase

# Firebase auth config (contains user data)
auth-config.json

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# turbopack
.turbo
/.idea/inside-out.iml
/.idea/vcs.xml
/.idea/modules.xml

# Custom agents (user-created agents should not be committed)
# Note: index.ts IS committed (with empty export) to avoid build failures
/src/config/custom-agents/*.json
